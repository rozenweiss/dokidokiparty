import React from "react";
import ReactDOM from "react-dom/client";
import GuildPartyMatcherAdmin from "./GuildPartyMatcherAdmin";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GuildPartyMatcherAdmin />
  </React.StrictMode>
);
